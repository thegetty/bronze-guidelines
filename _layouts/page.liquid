---
## CUSTOMIZED FILE -- Bronze Guidelines
## Adds iframe image viewer
##
classes:
  - quire-page
layout: base.11ty.js
description: Single page default template
---

{% pageHeader
  byline_format=byline_format,
  image=image,
  label=label,
  pageContributors=pageContributors,
  subtitle=subtitle,
  title=title
%}
{% if content %}
<section class="section quire-page__content">
  <div class="container">
    <div class="content">
      {{ content }}
      {% bibliography citations %}
      <div class="iframe_viewer" id="iframe-viewer" style="display:none;">
        <div class="iframe_viewer__nav-buttons" id="iframe-nav"></div>
        <div class="iframe_viewer__controls">
          <a class="iframe-control--close" aria-label="Close" onclick="toggleViewer()"></a>
        </div>
        <iframe class="iframe_viewer__iframe" id="object-iframe" name="object-iframe"></iframe>
      </div>
    </div>
  </div>
</section>
{% endif %}
<section class="section quire-page__content">
  <div class="container">
    {% pageButtons pagination=pagination %}
  </div>
</section>
